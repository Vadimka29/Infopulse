
public class Cezar {
	private CircleList<Character> cl;
	//сдвиг
	private int shift;
	
	public Cezar(int sh){
		shift = sh;
		cl = new CircleList<>();
		for(char temp = 'z'; temp >= 'a'; temp--){
			cl.addFirst(temp);
		}
		
	}
}
